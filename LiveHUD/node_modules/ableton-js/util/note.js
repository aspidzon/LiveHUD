export const tupleToNote = (tuple) => ({
    pitch: tuple[0],
    time: tuple[1],
    duration: tuple[2],
    velocity: tuple[3],
    muted: tuple[4],
});
export const noteToTuple = (note) => [
    note.pitch,
    note.time,
    note.duration,
    note.velocity,
    note.muted,
];
